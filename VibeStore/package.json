{
  "name": "vibestore",
  "version": "9.2",
  "description": "VibeStore - Advanced admin dashboard with real-time statistics, newsletter management, and user reports system (No Cloud Functions - 100% Free Tier)",
  "main": "index.js",
  "scripts": {
    "dev": "bundle exec jekyll serve --port 4000",
    "build": "bundle exec jekyll build",
    "build:optimized": "npm run build && npm run optimize:css && npm run optimize:images",
    "clean:production": "node clean-production.js",
    "build:production": "npm run clean:production && npm run build:optimized",
    "optimize:css": "npm run minify:css",
    "optimize:images": "node optimize-images.js",
    "minify:css": "npm run build && npx clean-css-cli -o assets/css/main.min.css assets/css/main.css",
    "version:check": "node version-check.js check",
    "version:sync": "node version-check.js sync",
    "deploy:smart": "../smart-deploy.sh",
    "deploy:safe": "npm run version:check && npm run build && npx firebase deploy --only hosting",
    "firebase:login": "npx firebase login",
    "firebase:init": "npx firebase init",
    "firebase:serve": "npm run build && npx firebase emulators:start --only hosting,functions,firestore,auth",
    "firebase:deploy:functions": "npx firebase deploy --only functions",
    "firebase:deploy:hosting": "npm run build && npx firebase deploy --only hosting",
    "firebase:deploy": "npm run build && npx firebase deploy",
    "functions:dev": "cd functions && npm run serve",
    "functions:deploy": "cd functions && npm run deploy",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-tools": "^14.17.0"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.2",
    "imagemin": "^8.0.1",
    "imagemin-webp": "^8.0.0",
    "imagemin-mozjpeg": "^10.0.0",
    "imagemin-pngquant": "^9.0.2"
  }
}
